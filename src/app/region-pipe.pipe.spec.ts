import { RegionPipePipe } from './region-pipe.pipe';

describe('RegionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RegionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
